@model IEnumerable<TheTjerrs.Models.Pyetja>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Pytja)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Pytja)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PyetjaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PyetjaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PyetjaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
